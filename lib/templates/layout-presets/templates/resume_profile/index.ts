import type { TemplateLayoutRegistry } from "../../types";
import prime from "./prime";

const resumeProfileLayouts: TemplateLayoutRegistry = {
  templateKey: "resume_profile",
  layouts: [prime],
};

export default resumeProfileLayouts;