import type { TemplateLayoutRegistry } from "../../types";
import savage from "./savage";

const roastLayouts: TemplateLayoutRegistry = {
  templateKey: "roast_session",
  layouts: [savage],
};


export default roastLayouts;