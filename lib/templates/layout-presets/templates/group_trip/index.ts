import type { TemplateLayoutRegistry } from "../../types";
import dolce from "./dolce";

const groupTripLayouts: TemplateLayoutRegistry = {
  templateKey: "group_trip",
  layouts: [dolce],
};

export default groupTripLayouts;