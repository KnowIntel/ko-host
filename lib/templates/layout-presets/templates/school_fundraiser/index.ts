import type { TemplateLayoutRegistry } from "../../types";
import futures from "./futures";

const schoolFundraiserLayouts: TemplateLayoutRegistry = {
  templateKey: "school_fundraiser",
  layouts: [futures],
};

export default schoolFundraiserLayouts;