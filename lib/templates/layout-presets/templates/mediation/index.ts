import type { TemplateLayoutRegistry } from "../../types";
import shared from "./shared";

const mediationLayouts: TemplateLayoutRegistry = {
  templateKey: "mediation",
  layouts: [shared],
};

export default mediationLayouts;