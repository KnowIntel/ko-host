import { createLayoutPreset } from "../../shared";

export default createLayoutPreset({
  designKey: "direct",

  label: "Direct",
  description: "Clean, direct networking layout for clubs, groups, meetups, and shared-interest communities.",
  thumbnail: "/designs/design-cards/design-card_enthusiast_networking_direct.webp",

  recommended: true,
});